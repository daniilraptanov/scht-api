#include <iostream>
#include "user_service.h"
#include "user.h"

using namespace std;

class UserService
{
    public:
        UserService();

        User get_user_by_email(string email)
        {

        };

        bool create_or_update_user(User user)
        {

        };

        bool remove_user_by_id(string id)
        {

        };
};


